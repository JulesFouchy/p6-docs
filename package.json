{
  "name": "p6-docs",
  "version": "0.0.0",
  "scripts": {
    "start": "cd ./website && npm run start",
    "build": "cd ./website && npm run build",
    "deploy": "cd ./website && npm run deploy"
  }
}
