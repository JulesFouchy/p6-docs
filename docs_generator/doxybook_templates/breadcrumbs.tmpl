{% if exists("moduleBreadcrumbs") -%}
**Section:** {%- for module in moduleBreadcrumbs -%}
 **[{{module.title}}]({{module.url}})**{% if not loop.is_last %} **/** {% endif -%}
{% endfor %}

{% endif -%}